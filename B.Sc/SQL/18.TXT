CREATE TABLE PURCHASE(ORDER_ID NUMBER(4) PRIMARY KEY,
PRODUCT_ID NUMBER(1),
UNIT_PRICE NUMBER(5),
QTY NUMBER(3));

INSERT INTO PURCHASE VALUES(5555,2,99,1);

SELECT * FROM PURCHASE WHERE UNIT_PRICE<100 ;

UPDATE PURCHASE SET UNIT_PRICE=200 WHERE PRODUCT_ID=2;